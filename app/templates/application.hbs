{{#if enableHeader}}
  {{crucible-header session=session invalidateSession="invalidateSession" openModal="openModal"}}
{{/if}}
{{outlet}}
{{#if enableFooter}}
  {{crucible-footer}}
{{/if}}

{{outlet 'modal'}}

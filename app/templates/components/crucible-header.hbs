<header>
  <div class="navbar-header">
    <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target=".bs-navbar-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    {{#link-to 'index' classNames='navbar-brand'}}CRUCIBLE{{/link-to}}
  </div>

  <nav class="collapse navbar-collapse">
    <ul class="nav navbar-nav pull-right">
      <li><a class="navButton" href="{{!-- <% if user_signed_in? %>#/servers/new<% else %>/#<%end%> --}}">Single Server Test</a></li>
      <li><a class="navButton" href="#">Multi Server Test</a></li>
      {{#if session.isAuthenticated}}
        <li><a href="#" class="navButton" {{action 'invalidateSession'}}>Logout</a></li>
      {{else}}
        <li><a href="/users/login" class="navButton" {{action 'openLoginModal'}}>Login</a></li>
      {{/if}}
    </ul>
  </nav>
</header>
